# Enable Powerlevel10k instant prompt. Should stay close to the top of ~/.zshrc.
# Initialization code that may require console input (password prompts, [y/n]
# confirmations, etc.) must go above this block; everything else may go below.
if [[ -r "${XDG_CACHE_HOME:-$HOME/.cache}/p10k-instant-prompt-${(%):-%n}.zsh" ]]; then
  source "${XDG_CACHE_HOME:-$HOME/.cache}/p10k-instant-prompt-${(%):-%n}.zsh"
fi

# Plugins {{{
# ==============================================================================

    # Plugins are declared in zsh_plugins.txt and compiled once into a static
    # zsh_plugins.zsh. Regenerating only when the list changes keeps startup fast.
    zsh_plugins=~/.zsh_plugins
    if [[ ! ${zsh_plugins}.zsh -nt ${zsh_plugins}.txt ]]; then
      source ~/.antidote/antidote.zsh
      antidote bundle <${zsh_plugins}.txt >|${zsh_plugins}.zsh
    fi
    source ${zsh_plugins}.zsh
    unset zsh_plugins

# }}}

source ~/.zshrc_alias

# Add machine specific settings in separate file.
if [[ -e ~/.zsh_local ]]; then
  source ~/.zsh_local
fi

export PATH=$HOME/.toolbox/bin:$PATH

# fzf, nvm and p10k are already loaded by ~/.zshenv, which zsh sources before
# this file. Loading them again here just doubles startup cost.


# BEGIN Agency MANAGED BLOCK
if [[ ":${PATH}:" != *":/home/rishisharma/.config/agency/CurrentVersion:"* ]]; then
    export PATH="/home/rishisharma/.config/agency/CurrentVersion:${PATH}"
fi
# END Agency MANAGED BLOCK
